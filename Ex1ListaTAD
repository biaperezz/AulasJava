1- Implemente uma classe Lista que use um vetor de inteiros para armazenar até 5 elementos.
Adicione os métodos inserir(int valor) e mostrar().

package Estruturas;

public class Lista {
    private int[] elementos = new int[5];
    private int tamanho = 0;

    public void inserir(int valor) {
        if (tamanho < elementos.length) {
            elementos[tamanho] = valor;
            tamanho++;
            System.out.println("-> Inserido " + valor + " na posição " + (tamanho - 1));
        } else {
            System.out.println("Lista cheia! Não é possível inserir mais elementos.");
        }
    }

    public int removerPorIndice(int indice) {
        if (indice < 0 || indice >= tamanho) {
            return -1;
        }
        int valorRemovido = elementos[indice];
        for (int i = indice; i < tamanho - 1; i++) {
            elementos[i] = elementos[i + 1];
        }
        tamanho--;
        return valorRemovido;
    }

    public int getTamanho() {
        return tamanho;
    }

    public void mostrar() {
        System.out.print("Lista: ");
        if (tamanho == 0) {
            System.out.println("Vazia");
            return;
        }
        for (int i = 0; i < tamanho; i++) {
            System.out.print(elementos[i] + (i < tamanho - 1 ? " | " : ""));
        }
        System.out.println();
    }
}
